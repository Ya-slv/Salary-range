# Руководство для Backend-разработчика (backend_readme.md)

В данном файле описана архитектура бэкенда, эндпоинты, интеграция с локальной LLM и механизм горячей перезагрузки ресурсов.

## 1. Технологический стек
* **Бэкенд-фреймворк**: Django 5.0 + Django REST Framework (DRF)
* **Документация API**: `drf-spectacular` (Swagger UI доступен по адресу `/api/docs/`)
* **Среда выполнения тестов**: `pytest` + `pytest-django`

## 2. API Эндпоинты (`views.py` & `serializers.py`)
1. **`POST /api/calculate_salary/`**
   * Принимает структурированные данные профиля (`role`, `experience_year`, `skills`, `region`, `schedule`, `education`).
   * Передаёт данные в ML-сервис и возвращает зарплатную вилку (`min_salary`, `max_salary`) вместе со списком рекомендованных навыков.
2. **`POST /api/parse_resume/`**
   * Принимает файл резюме (до 5 МБ, форматы: PDF, DOCX, DOC, TXT) через `multipart/form-data`.
   * Извлекает текст (с помощью `pdfplumber` для PDF и `python-docx` для DOCX).
   * Вызывает локальную языковую модель для структурирования информации.

## 3. Парсер резюме на базе локальной LLM (`services.py`)
Интеграция с локальной языковой моделью реализована через библиотеку `llama-cpp-python`:
* **Модель**: `qwen2.5-0.5b-instruct-q4_k_m.gguf` (лежит в `backend/salary/api/models/`).
* **Формат промпта**: **ChatML** (`<|im_start|>system...<|im_end|>`), настроенный строго на возврат JSON-объекта без разметки markdown.
* **Очистка данных (`clean_json_response`)**:
  * Извлекает сырой JSON-блок из ответа модели.
  * Реализует запасные сопоставления ключей (например, если модель выдаст `experience_years` или `experience` вместо целевого поля `experience_year`).

## 4. Динамическая перезагрузка ML ресурсов (`ml_service.py`)
Для того чтобы бэкенд не требовал ручного перезапуска после переобучения модели, реализована горячая перезагрузка ресурсов в `load_ml_resources`:
* Функция сохраняет метки времени (`mtime`) файлов `data/model.cbm` и `data/vacancies.csv`.
* При каждом запросе расчёта зарплаты проверяется текущее время изменения файлов на диске.
* Если дата изменения файла свежее сохранённой (например, модель была переобучена скриптом), ресурсы автоматически перезагружаются в оперативную память на лету.
* Чтение вакансий выполняется с помощью pandas с автоматическим определением разделителя.
